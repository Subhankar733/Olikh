<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:label="OLIKH"
        android:theme="@style/Theme.Olikh"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:supportsPictureInPicture="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

            <activity
            android:name=".AdvancedBrowserHubActivity"
            android:exported="false" />

        <activity
            android:name=".BrowserDiagnosticsActivity"
            android:exported="false" />

        <activity
            android:name=".ReaderModeActivity"
            android:exported="false" />

        <activity
            android:name=".PageToolsActivity"
            android:exported="false" />

        <activity
            android:name=".DownloadManagerActivity"
            android:exported="false" />

        <activity
            android:name=".SavedPagesActivity"
            android:exported="false" />

        <activity
            android:name=".PrivacyDataActivity"
            android:exported="false" />

    </application>

</manifest>
