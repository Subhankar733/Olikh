<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#07090E">

    <!-- Immersive Web Canvas -->
    <FrameLayout
        android:id="@+id/browserContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <WebView
            android:id="@+id/webView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:overScrollMode="never" />
    </FrameLayout>

    <!-- Glowing Top Progress Indicator -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_gravity="top"
        android:max="100"
        android:progressTint="#00F0FF"
        android:visibility="gone" />

    <!-- Legacy Reference Holder -->
    <LinearLayout
        android:id="@+id/browserTopBar"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        android:orientation="horizontal" />

    <!-- Modern 2-Tier Floating HUD Control Deck -->
    <LinearLayout
        android:id="@+id/browserBottomDock"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:orientation="vertical"
        android:background="@drawable/bg_glass_capsule"
        android:elevation="14dp"
        android:padding="8dp">

        <!-- Tier 1: Spacious Smart Omnibox -->
        <LinearLayout
            android:id="@+id/addressBarContainer"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:gravity="center_vertical"
            android:background="@drawable/bg_capsule_pill"
            android:paddingStart="12dp"
            android:paddingEnd="10dp">

            <ImageView
                android:id="@+id/sslLockIcon"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/ic_search"
                app:tint="#00F0FF"
                android:contentDescription="Status" />

            <EditText
                android:id="@+id/addressBar"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:layout_marginStart="10dp"
                android:layout_marginEnd="6dp"
                android:background="@android:color/transparent"
                android:hint="Search or enter web address..."
                android:inputType="textUri"
                android:imeOptions="actionGo"
                android:singleLine="true"
                android:ellipsize="middle"
                android:textColor="#FFFFFF"
                android:textColorHint="#64748B"
                android:textSize="14sp"
                android:padding="0dp"
                android:selectAllOnFocus="true"
                android:importantForAutofill="no" />

            <ImageButton
                android:id="@+id/btnReload"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_reload"
                app:tint="#94A3B8"
                android:padding="6dp"
                android:contentDescription="Reload" />
        </LinearLayout>

        <!-- Tier 2: Balanced Navigation & Tool Matrix -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="46dp"
            android:layout_marginTop="6dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:weightSum="5">

            <ImageButton
                android:id="@+id/btnBack"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_back"
                app:tint="#94A3B8"
                android:padding="11dp"
                android:contentDescription="Back" />

            <ImageButton
                android:id="@+id/btnForward"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_forward"
                app:tint="#94A3B8"
                android:padding="11dp"
                android:contentDescription="Forward" />

            <ImageButton
                android:id="@+id/btnNewTab"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_add"
                app:tint="#00F0FF"
                android:padding="11dp"
                android:contentDescription="New Tab" />

            <FrameLayout
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1">

                <Button
                    android:id="@+id/btnTabs"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:layout_gravity="center"
                    android:minWidth="0dp"
                    android:minHeight="0dp"
                    android:padding="0dp"
                    android:background="@drawable/bg_capsule_pill"
                    android:text="1"
                    android:textColor="#00F0FF"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:contentDescription="Tabs" />
            </FrameLayout>

            <ImageButton
                android:id="@+id/btnMenu"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_more_vertical"
                app:tint="#94A3B8"
                android:padding="11dp"
                android:contentDescription="Menu" />
        </LinearLayout>

        <!-- Hidden Legacy Hooks -->
        <ImageButton
            android:id="@+id/btnHome"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone" />

        <ImageButton
            android:id="@+id/btnBookmark"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone" />
    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
